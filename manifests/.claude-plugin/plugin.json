{
  "name": "alexandria",
  "version": "1.0.0",
  "description": "Active context management for coding agents. Provides wiki search, ingest, lint, and initialization via MCP tools.",
  "skills": "./skills/",
  "mcpServers": {
    "alexandria": {
      "command": "bun",
      "args": ["${PLUGIN_PATH}/src/index.ts"],
      "env": {}
    }
  }
}
